---
import MainToken from '../token.astro';

export function getStaticPaths() {
  return [
    { params: { lang: 'de' } },
    // { params: { lang: 'fr' } }, // French temporarily disabled
  ];
}
---

<MainToken />
