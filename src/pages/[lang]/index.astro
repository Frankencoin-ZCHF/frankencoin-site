---
import MainIndex from '../index.astro';

export const prerender = false;

export function getStaticPaths() {
  return [
    { params: { lang: 'de' } },
    // { params: { lang: 'fr' } }, // French temporarily disabled
  ];
}
---

<MainIndex />
